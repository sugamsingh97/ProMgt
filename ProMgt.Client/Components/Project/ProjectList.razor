
@foreach (var item in Items)
{
    <ProjectItem Item="@item" />
}

@code {
    [Parameter]
    public List<ProjectDisplay> Items { get; set; } = new();
    protected async override Task OnInitializedAsync()
    {

        await base.OnInitializedAsync();
    }
}
